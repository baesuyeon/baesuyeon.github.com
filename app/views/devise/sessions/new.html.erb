<!DOCTYPE html>
<html>
    <head>
    </head>
    <body class="main_logo su">

      <div >
        <div class="su_top su_margin">
          <span class="bigbig">티므빌디잉의 계정을 입력해주세용</span>
        </div>
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <br />
            <div class="em">
              <%= f.label :email, class: 'e_title'%>&nbsp;
              
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'e_input'%>
            </div>
            <br />
          
            <div>
              <%= f.label :password, class: 'e_title' %>&nbsp;
              <%= f.password_field :password, autofocus: true, autocomplete: "off", class: 'e_input' %>
            </div>
            <br />
          
            <% if devise_mapping.rememberable? %>
              <div>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </div>
            <% end %>
            
            <div>
              <%= f.submit "접 속", class:"connect_btn" %>
              
              <br><br><br>
            <p class="main_button">
              <p>혹시라도 아직 회원이 아니라면?</p>
            <button type="button" class="btn_mini" onclick="location.href='/users/sign_up'">회원 가입</a></button>
            </p>
            </div>
            
          <% end %>
      </div>
<%#= render "devise/shared/links" %>
        
       
    </body>
</html>